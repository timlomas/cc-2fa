document.addEventListener("DOMContentLoaded", function () {
  const form = document.querySelector("#cc2fa-form");

  if (form) {
    form.addEventListener("submit", function (event) {
      // Add custom form submission handling here if needed
      // e.g., form validation before submitting
    });
  }
});
